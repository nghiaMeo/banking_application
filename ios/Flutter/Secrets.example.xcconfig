// Copy to Secrets.xcconfig in this folder (Secrets.xcconfig is gitignored).
// Use the same API key as MAPS_API_KEY in android/local.properties if both
// Maps SDK for Android and Maps SDK for iOS are enabled for that key.
MAPS_API_KEY=PASTE_YOUR_KEY_HERE
